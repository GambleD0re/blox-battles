// /backend/src/app.js

import express from 'express';

const app = express();

// Global middleware will be added here in subsequent steps.
// For now, it's a clean slate for the server to run.

export default app;
